
import { testBig } from 'dummy/helpers/test-big';
import { module, test } from 'qunit';

module('Unit | Helper | test big');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = testBig([42]);
  assert.ok(result);
});

